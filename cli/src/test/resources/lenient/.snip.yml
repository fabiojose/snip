description: |
  Describe your templation
placeholders:
  strict: no # any placeholder may absent
  spec:
    - name: __c_author_
      pattern: ".+"
      label: Author Name
    - name: __c_domain_
      pattern: '[A-Z][\w]+'
      label: Class name for Domain
post:
  script:
    linux: # base directory is the appdir
      - echo linux
    windows:
      - echo windows
